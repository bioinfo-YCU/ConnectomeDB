---
title: ""

execute:
  echo: false
  warnings: false  # Add this line to suppress warnings globally
toc: false
format:
  html:
    embed-resources: true
    code-fold: false
    html-math-method: webtex
    keep-md: true
    allow-html: true
---

```{=html}
<style>
/* Ensure the entire viewport is used and prevent root scrolling */
html, body {
    height: 100%; /* Changed from 80% to 100% */
    margin: 0;
    padding: 0;
    overflow: hidden; /* Prevent overall page scrolling on larger screens */
}

/* Make body the primary flex container for the page layout */
body {
    display: flex;
    flex-direction: column; /* Stack header, main, and footer vertically */
    height: 100vh; /* Ensure body takes full viewport height */
}

/* Optional: Style for a header if you have one */
.page-header {
    flex: 0 0 auto; /* Header takes its natural height and doesn't grow/shrink */
    padding: 1rem;
    background-color: #e0e0e0;
    text-align: center;
}

/* Adjust main to fill the available space */

main {
    display: flex;
    flex-direction: column;
    height: 100%;
    overflow: hidden;
}


/* Main content area should grow to take available space within 'main' */
.main-content {
    flex: 1; /* Allows this element to grow and shrink within 'main' */
    display: flex; /* Keep flexbox for its internal layout if needed */
    flex-direction: column;
    overflow: visible; /* Allow content to be visible */
    min-height: 0; /* Important for flex items to shrink correctly */
    padding: 1rem; /* Add some padding back for content */
}

/* For carousel - ensure it fits within its allocated space within 'main' */
.carousel-section {
    flex: 0 0 auto; /* It retains its intrinsic height/width */
    width: 80%; /* Ensure full width usage */
    max-height: 300px !important; 
    overflow: visible; /* Allow carousel content to be fully visible */
    display: flex;
    align-items: center; /* Center carousel content vertically */
    justify-content: center; /* Center carousel content horizontally */
    min-height: fit-content; /* Allow natural sizing */
}

/* Add specific sizing for carousel content */
.carousel-section > * {
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
}

/* Style for the footer to ensure it's always visible at the bottom of the page */
.page-footer {
    flex: 0 0 auto; /* Footer should take its natural height and not grow or shrink */
    padding: 1rem; /* Example padding */
    background-color: #f0f0f0; /* Example background color */
    text-align: center; /* Example alignment */
}

/* General responsive adjustments */
@media (max-width: 768px) {
    .main-content {
        padding: 0.5rem; /* Reduce padding on mobile for more space */
    }
    
    .page-header, .page-footer {
        padding: 0.5rem; /* Reduce header/footer padding on mobile */
    }
}

/* Additional breakpoint for very small screens */
@media (max-width: 480px) {
    .main-content {
        padding: 0.25rem; /* Even less padding on very small screens */
    }
    
    .page-header, .page-footer {
        padding: 0.25rem;
    }
}

</style>
```

```{=html}

{{< include HTML/image_carousel_mobileNoCarousel.html >}}

```